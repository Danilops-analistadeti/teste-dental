import { FormatDateTime } from '../interfaces/format-date-time.interface';

export const FORMAT_DATETIME: FormatDateTime = {
    year: 'numeric',
    day: '2-digit',
    month: '2-digit',
    hour: 'numeric',
    minute: 'numeric'
};
