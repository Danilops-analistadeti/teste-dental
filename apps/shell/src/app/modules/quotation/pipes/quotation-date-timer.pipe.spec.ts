import { QuotationDateTimerPipe } from './quotation-date-timer.pipe';

describe('DateTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotationDateTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
